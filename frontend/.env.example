# Copy to .env.local for local dev (optional — proxy handles it).
# Set in Vercel environment for production builds.
#
# Empty/absent = use the /api proxy (dev default).
# Production   = full https URL of the Railway backend.
VITE_API_BASE=
